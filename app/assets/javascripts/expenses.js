
// wait for document ready
$(funtion(){
  
})

// need to add a serializer for expenses
//  need to render User Expenses index page via JSON/JS
  //so...hijack a button?
  // fire ajax request to get data 
  // when server response with JSON iterate over the JSON data and inject into DOM
//  need to  add a next button to Expenses show page to serve up next expense via JSON/JS
// need to render one has many relationship via JSON using JS - each user has many expenses - show those on user show page?
  //  or need to create a new category show page per user and render expenses per category via ajax
// need to render a form for a new expense that submits dynamically
// translate JSON response into JS Model Object